apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stats-matomo-db-pvc
  namespace: stats
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 85Gi
  storageClassName: ""
  volumeName: srv-stats-matomo-db-local-pv
