apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stats-matomo-files-pvc
  namespace: stats
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: ""
  volumeName: srv-stats-matomo-files-local-pv
