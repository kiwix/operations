apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: services-drives-files-pvc
  namespace: drives
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi
  storageClassName: ""
  volumeName: srv-services-drives-files-local-pv
