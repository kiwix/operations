apiVersion: v1
kind: PersistentVolume
metadata:
  name: srv-stats-matomo-maria-db-local-pv
spec:
  capacity:
    storage: 200Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /data/matomo/mariadb/files
  claimRef:
    name: stats-matomo-maria-db-pvc
    namespace: stats
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: k8s.kiwix.org/role
              operator: In
              values:
              - stats
