apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zammad-elasticsearch-pvc
  namespace: zammad
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: local-storage
  volumeName: services-zammad-elasticsearch-local-pv
