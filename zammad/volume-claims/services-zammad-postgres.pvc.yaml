apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: services-zammad-postgres-pvc
  namespace: zammad
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: local-storage
  volumeName: srv-services-zammad-postgres-local-pv
