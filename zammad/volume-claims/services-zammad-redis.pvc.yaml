apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: services-zammad-redis-pvc
  namespace: zammad
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: local-storage
  volumeName: srv-services-zammad-redis-local-pv
