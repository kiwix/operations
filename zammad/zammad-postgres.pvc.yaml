apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zammad-postgres-pvc
  namespace: zammad
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: local-storage
  volumeName: services-zammad-postgres-local-pv
