apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: browser-archive-pvc
  namespace: zim
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Ti
  storageClassName: local-storage
  volumeName: srv-b-archive-local-pv
