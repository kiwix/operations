apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: master-openzim-nightly-pvc
  namespace: zim
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100G
  storageClassName: local-storage
  volumeName: srv-m-openzim-nightly-local-pv
