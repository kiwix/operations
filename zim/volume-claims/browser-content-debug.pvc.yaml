apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: browser-content-debug-pvc
  namespace: zim
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage
  volumeName: srv-b-content-debug-local-pv
