apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: browser-www-logs-pvc
  namespace: zim
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1G
  storageClassName: local-storage
  volumeName: srv-b-www-logs-local-pv
