apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: master-maps-pvc
  namespace: zim
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: local-storage
  volumeName: srv-m-www-maps-local-pv
