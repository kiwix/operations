apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: services-ui-db-pvc
  namespace: mirrors-qa
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage
  volumeName: srv-services-mirrors-qa-ui-db-local-pv
