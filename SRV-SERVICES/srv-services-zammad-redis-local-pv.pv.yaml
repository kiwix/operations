apiVersion: v1
kind: PersistentVolume
metadata:
  name: srv-services-zammad-redis-local-pv
spec:
  capacity:
    storage: 8Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /data/zammad/redis
  claimRef:
    name: services-zammad-redis-pvc
    namespace: zammad
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: k8s.kiwix.org/role
              operator: In
              values:
              - services
