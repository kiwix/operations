apiVersion: v1
kind: PersistentVolume
metadata:
  name: srv-b-hidden-local-pv
spec:
  capacity:
    # content is ~9.5TB ATM (Jan 2026)
    storage: 12Ti
  volumeMode: Filesystem
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /data/hidden
  claimRef:
    name: browser-hidden-pvc
    namespace: zim
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: k8s.kiwix.org/role
              operator: In
              values:
              - browser
