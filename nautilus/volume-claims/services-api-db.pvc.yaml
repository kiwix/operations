apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: services-api-db-pvc
  namespace: nautilus
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage
  volumeName: srv-services-nautilus-api-db-local-pv
