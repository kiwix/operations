apiVersion: batch/v1
kind: CronJob
metadata:
  name: master-logrotate
  namespace: zim
spec:
  schedule: "0 1 * * *"
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: logrotate
              image: docker.io/blacklabelops/logrotate:1.3
              imagePullPolicy: IfNotPresent
              resources:
                requests:
                  memory: "128Mi"
                  cpu: "100m"
                limits:
                  memory: "128Mi"
                  cpu: "100m"
              volumeMounts:
              - mountPath: "/data/nginx-logs"
                name: nginx-logs-volume
                readOnly: false
              - mountPath: "/etc/logrotate.conf"
                name: logrotate-configs
                subPath: logrotate.conf
                readOnly: true
              - mountPath: "/etc/logrotate.d/mirror-download-kiwix.conf"
                name: logrotate-configs
                subPath: mirror-download-kiwix.conf
                readOnly: true
              command: ["logrotate", "-v", "--state", "/data/nginx-logs/logrotate.status", "/etc/logrotate.conf"]
          volumes:
          - name: nginx-logs-volume
            persistentVolumeClaim:
              claimName: master-www-logs-pvc
          - name: logrotate-configs
            configMap:
              name: logrotate-configs
          restartPolicy: OnFailure
          nodeSelector:
            k8s.kiwix.org/role: "master"
