apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: services-api-db-pvc
  namespace: cardshop
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage
  volumeName: srv-services-cardshop-api-db-local-pv
