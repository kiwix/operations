apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: login
  labels:
    app: ory-admin-ui-prod-app
  name: ory-admin-ui-deployment-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ory-admin-ui-prod-app
  template:
    metadata:
      labels:
        app: ory-admin-ui-prod-app
    spec:
      containers:
      - image: dhiagharsallaoui/kratos-admin-ui:latest
        imagePullPolicy: Always
        name: ory-admin-ui
        envFrom:
        - configMapRef:
            name: ory-admin-ui-prod-configs
        - secretRef:
            name: ory-admin-ui-prod-secrets
        ports:
        - containerPort: 3000
        resources:
          requests:
            memory: "1Gi"
            cpu: "20m"
      nodeSelector:
        k8s.kiwix.org/role: "services"