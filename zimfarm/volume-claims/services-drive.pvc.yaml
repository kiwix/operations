apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: services-drive-pvc
  namespace: zimfarm
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 64Gi
  storageClassName: ""
  volumeName: srv-services-zimfarm-drive-local-pv
