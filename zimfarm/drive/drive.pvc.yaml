apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: drive-local-pvc
  namespace: zimfarm
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 64Gi
  storageClassName: ""
  volumeName: zimfarm-drive-local-pv
