apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: services-db-pvc
  namespace: cms
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage
  volumeName: srv-services-cms-db-local-pv
