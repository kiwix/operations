apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: browser-hotspot-drive-pvc
  namespace: offspot
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: local-storage
  volumeName: srv-b-hotspot-drive-local-pv
